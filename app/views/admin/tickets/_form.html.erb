<%= form_for [:admin, @ticket] do |form| %>
  <%= render 'common/errors', resource: @ticket %>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number %>
  </div>

  <dev class="field">
    <%= form.label :train_id, "Train" %>
    <%= form.collection_select :train_id, Train.all, :id, :number %>
  </dev>

  <dev class="field">
    <%= form.label :start_station_id, "Start Stations" %>
    <%= form.collection_select :start_station_id, RailwayStation.all, :id, :title %>
  </dev>

  <dev class="field">
    <%= form.label :end_station_id, "End Stations" %>
    <%= form.collection_select :end_station_id, RailwayStation.all, :id, :title %>
  </dev>

  <div class="actions">
    <%= form.submit "#{ @ticket.new_record? ? 'create' : 'edit' }" %>
  </div>
<% end %>